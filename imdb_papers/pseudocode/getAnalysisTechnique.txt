Knowns Techniques:
INAA - instrumental neutron activation analysis
RNAA - radiochemical neutron activation analysis

Keywords:
[Analysis, analyses, analytical, technique, instrumental, radiochemical]

Typical Location Sections:
1. Introduction
2. Section 2

----------------------- Start Pseudocode--------------------------
import nltk
from nltk.tokenize import word_tokenize

#tokenize function
def preprocess(sent):
	sent = nltk.word_tokenize(sent)
	return sent


# get resource
pdf = uploadResource(target)

# preprocess the resource
processed = preprocess(pdf)

# list to hold matches to RNAA or INAA
techniques[]

# search for matches in processed array
def findTechnique(processed)
	for x in processed:
		if (preprocessed[x] == 'RNAA' or preprocessed[x] == 'INAA')
		techniques.append(preprocess[x])

***This will work on most newer resources. Some papers do have references 
to both INAA and RNAA. Typically INAA has replaced RNAA so it can almost be assumed
that if a PDF contains both INAA and RNAA that INAA is the analysis technique.

# Count matches for RNAA or INAA
for x in techniques:
	if (techniques[x] == "RNAA"):
		rnaa_count++;
	else:
		inaa_count++;

	if (rnaa_count > 0 and inaa_count == 0):
		technique = "RNAA"
	else
		technique = "INAA"


